# GENERATED by bin/tn-render. Edit templates/.config/bat/config.in.
# The theme is a .tmTheme fetched into ~/.config/bat/themes/ by steps/65-theme.sh.
# `bat cache --build` must run after any bat upgrade or the theme silently
# vanishes with "unknown theme" — bin/tn-render does this for you.
--theme=tokyonight_night
--style=numbers,changes,header
--italic-text=always
